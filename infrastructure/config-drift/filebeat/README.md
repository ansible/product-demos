# Filebeat configuration for config drift demo
#
# Stage 2 will add:
#   - filebeat.yml (base config)
#   - auditd.yml (auditd module)
#
# Stage 2 debug: output.console.pretty: true
# Stage 3:       output.kafka → linux-audit-events
